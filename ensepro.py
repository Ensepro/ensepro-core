"""
@project ensepro
@since 25/02/2018
@author Alencar Rodrigo Hentges <alencarhentges@gmail.com>

"""

"""
RASCUNHO DE PARAMETROS



-f, --file PATH -> indica um arquivo contendo frases a ser processado
-s, --save PATH -> indica o arquivo onde será salvo todos os resultados
-ms, --mostrar-sinonimos -> se presente, irá fazer a busca e mostrar dos sinonimos das palavras relevantes

-m, --frase FRASE -> indica a frase que será processada
-q, --quiet -> indica que não deve mostrar nada no console e somente processar
-v, --verboso -> indica que deve mostrar todas as informações possíveis no log 
-esi, --elastic-search-index INDEX -> indica o INDEX que será utilizado no elasticsearch
"""

# 1. Definir parâmetros com o ArgumentParser -> https://martin-thoma.com/how-to-parse-command-line-arguments-in-python/
# 2. Setar o PATH do Ensepro no sys.path
#
#



