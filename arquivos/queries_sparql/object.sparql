select
  "ELEM" as ?e
  if(str(?o) = "ELEM", "true", "false") as ?m
  "o" as ?f
  str(?s) as ?s
  str(?p) as ?p
  str(?o) as ?o
where {
  ?s ?p ?o
  filter(str(?o) = "ELEM" || regex(str(?o), "ELEM"))
}