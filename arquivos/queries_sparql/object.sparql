select
  "WORD" as ?e
  if(str(?o) = "WORD", "true", "false") as ?m
  "o" as ?f
  str(?s) as ?s
  str(?p) as ?p
  str(?o) as ?o
where {
  ?s ?p ?o
  filter(str(?o) = "WORD" || regex(str(?o), "WORD"))
}