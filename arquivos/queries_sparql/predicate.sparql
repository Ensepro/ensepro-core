 select
  "WORD" as ?e
  if(str(?p) = "WORD", "true", "false") as ?m
  "p" as ?f
  str(?s) as ?s
  str(?p) as ?p
  str(?o) as ?o
where {
  ?s ?p ?o
  filter(str(?p) = "WORD" || regex(str(?p), "WORD"))
}