select
  "ELEM" as ?e
  if(str(?s) = "ELEM", "true", "false") as ?m
  "s" as ?f
  str(?s) as ?s
  str(?p) as ?p
  str(?o) as ?o
where {
  ?s ?p ?o
  filter(str(?s) = "ELEM" || regex(str(?s), "ELEM"))
}