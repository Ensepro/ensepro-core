select
  "WORD" as ?e
  if(str(?s) = "WORD", "true", "false") as ?m
  "s" as ?f
  str(?s) as ?s
  str(?p) as ?p
  str(?o) as ?o
where {
  ?s ?p ?o
  filter(str(?s) = "WORD" || regex(str(?s), "WORD"))
}